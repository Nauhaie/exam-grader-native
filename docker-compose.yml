version: "3.9"
services:
  exam-grader:
    build: .
    environment:
      - DISPLAY=host.docker.internal:0
      - QT_QPA_PLATFORM=xcb
    volumes:
      - ./data:/app/data
      - ./sample_data:/app/sample_data
    network_mode: host
